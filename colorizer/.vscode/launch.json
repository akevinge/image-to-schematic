{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceRoot}/bin/main",
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb",
      "args": ["../textures-1.18.2", "../colors.json", "500"],
      "preLaunchTask": "build"
    }
  ]
}
